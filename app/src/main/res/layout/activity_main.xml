<?xml version="1.0" encoding="utf-8"?>
<!--
  Splash-екран Pulsa
  Calm wellness стилістика: кремовий фон, серце у м'якому кораловому крузі,
  назва, теглайн, 3 точки-індикатори. Анімація breathing heart на iconHeart
  запускається з SplashActivity (Kotlin).

  lottieLoading лишається невидимим — драйвить навігацію через
  addAnimatorUpdateListener у MainActivity (буде замінено пізніше).
-->
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/cream"
    tools:context=".presentation.MainActivity">

    <!-- Центральний контейнер: кружок із серцем + текст + точки -->
    <LinearLayout
        android:id="@+id/centerGroup"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:gravity="center_horizontal"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent">

        <FrameLayout
            android:id="@+id/heartCircle"
            android:layout_width="120dp"
            android:layout_height="120dp"
            android:background="@drawable/bg_circle_coral_light">

            <ImageView
                android:id="@+id/iconHeart"
                android:layout_width="56dp"
                android:layout_height="56dp"
                android:layout_gravity="center"
                android:src="@drawable/ic_heart"
                android:contentDescription="@string/app_name"
                app:tint="@color/coral" />
        </FrameLayout>

        <TextView
            android:id="@+id/appName"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="28dp"
            android:text="@string/app_name"
            android:textColor="@color/text_primary"
            android:textSize="32sp"
            android:letterSpacing="0.04"
            app:fontFamily="@font/rubik_semibold" />

        <TextView
            android:id="@+id/appTagline"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="6dp"
            android:text="@string/app_tagline"
            android:textColor="@color/text_secondary"
            android:textSize="13sp"
            app:fontFamily="@font/rubik" />

        <LinearLayout
            android:id="@+id/indicatorDots"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="32dp"
            android:orientation="horizontal"
            android:gravity="center">

            <View
                android:layout_width="6dp"
                android:layout_height="6dp"
                android:background="@drawable/bg_circle_coral" />

            <View
                android:layout_width="6dp"
                android:layout_height="6dp"
                android:layout_marginStart="6dp"
                android:alpha="0.45"
                android:background="@drawable/bg_circle_coral" />

            <View
                android:layout_width="6dp"
                android:layout_height="6dp"
                android:layout_marginStart="6dp"
                android:alpha="0.25"
                android:background="@drawable/bg_circle_coral" />
        </LinearLayout>

    </LinearLayout>

    <!-- Невидимий Lottie — драйвить навігацію до онбордингу.
         Буде замінено на Handler.postDelayed у SplashActivity. -->
    <com.airbnb.lottie.LottieAnimationView
        android:id="@+id/lottieLoading"
        android:layout_width="1dp"
        android:layout_height="1dp"
        android:alpha="0"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:lottie_fileName="loading.json" />

</androidx.constraintlayout.widget.ConstraintLayout>
